{
  "name": "test-sass",
  "version": "0.0.1",
  "description": "Prueba para css dev workflow",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass": "sass --watch src/scss:dist/css",
    "postcss": "postcss dist/css/style.css --replace",
    "css:dev": "npm run sass",
    "css:build": "sass src/scss:dist/css --style=compressed && npm run postcss"
  },
  "author": "@kkhenriquez",
  "license": "ISC",
  "devDependencies": {
    "@fullhuman/postcss-purgecss": "^1.2.0",
    "@lipemat/css-mqpacker": "^8.0.1",
    "postcss-cli": "^6.1.3",
    "postcss-preset-env": "^6.7.0",
    "sass": "^1.22.7"
  }
}
